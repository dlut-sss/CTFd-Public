version: "3"
services:
    service:
        image: ephemerally/checkin
        restart: always
        env_file:
          - .env
        environment:
          - FLAG=${FLAG}
        expose:
            - 80
        networks:
            - net

networks:
    # 配置docker network
    net: 
      external: 
         name: ctfd_frp_containers